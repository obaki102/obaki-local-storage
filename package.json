{
  "name": "obaki-local-storage",
  "version": "0.0.3",
  "description": " `obaki-local-storage` is a TypeScript utility for caching and retrieving data from the browser's `localStorage`. It provides a flexible and reusable function, allowing developers to cache data, automatically refresh it when needed, and define a custom staleness duration. Ideal for scenarios where you want to efficiently manage cached data in web applications.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsup"
  },
  "author": "obaki102",
  "license": "ISC",
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "jest-localstorage-mock": "^2.4.26",
    "mock-local-storage": "^1.1.24",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "crypto-js": "^4.2.0"
  }
}
